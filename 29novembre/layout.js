/**
 * 
 */
function benvenuto(user) {
	alert("Benvenuto " + user +"!");
}

function calcola(x, y, scelta) {
	var a = parseInt(x);
	var b = parseInt(y);
	var c = a + b;
	alert(c + " / " + scelta);
}